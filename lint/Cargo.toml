[package]
name = "darkbio-clock-lint"
version = "0.0.0"
description = "Checks the clippy configuration that darkbio-clock recommends to its users"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
crossbeam-channel = "0.5.17"
